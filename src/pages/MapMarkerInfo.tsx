import { IonCardTitle, IonContent, IonPage } from "@ionic/react"

const MapMarkerInfo = () => {
  return (
    <IonPage>
      <IonContent>
        <IonCardTitle>MapMarkerInfo</IonCardTitle>
      </IonContent>
    </IonPage>
  )
};

export default MapMarkerInfo;